apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-tmp
spec:
  capacity:
     storage: 2Gi
  volumeMode: Filesystem
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /tmp/postgresql/data
    type: DirectoryOrCreate
