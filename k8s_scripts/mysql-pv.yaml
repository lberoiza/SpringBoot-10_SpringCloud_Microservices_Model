apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv-tmp
spec:
  capacity:
     storage: 2Gi
  volumeMode: Filesystem
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /tmp/mysql
    type: DirectoryOrCreate
