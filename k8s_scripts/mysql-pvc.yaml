apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-tmp
spec:
  volumeName: mysql-pv-tmp
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi
